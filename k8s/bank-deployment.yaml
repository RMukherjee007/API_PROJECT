apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank
  namespace: nri-yield
spec:
  replicas: 1
  selector:
    matchLabels: { app: bank }
  template:
    metadata:
      labels: { app: bank }
    spec:
      containers:
      - name: bank
        image: nri-yield/bank-integration:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 8085
        envFrom:
        - configMapRef: { name: nri-config }
        - secretRef: { name: nri-secrets }
        livenessProbe:
          httpGet: { path: /health/live, port: 8085 }
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet: { path: /health/ready, port: 8085 }
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests: { cpu: "50m", memory: "64Mi" }
          limits: { cpu: "200m", memory: "128Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: bank
  namespace: nri-yield
spec:
  selector: { app: bank }
  ports:
  - port: 8085
    targetPort: 8085
    name: http
  type: ClusterIP
