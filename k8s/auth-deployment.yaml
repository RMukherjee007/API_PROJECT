apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
  namespace: nri-yield
spec:
  replicas: 1
  selector:
    matchLabels: { app: auth }
  template:
    metadata:
      labels: { app: auth }
    spec:
      containers:
      - name: auth
        image: nri-yield/auth:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 8083
        envFrom:
        - configMapRef: { name: nri-config }
        - secretRef: { name: nri-secrets }
        livenessProbe:
          httpGet: { path: /health/live, port: 8083 }
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet: { path: /health/ready, port: 8083 }
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests: { cpu: "50m", memory: "64Mi" }
          limits: { cpu: "200m", memory: "128Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: nri-yield
spec:
  selector: { app: auth }
  ports:
  - port: 8083
    targetPort: 8083
    name: http
  type: ClusterIP
